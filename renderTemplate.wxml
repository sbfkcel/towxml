<template name="m0"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m1" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m1"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m2" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m2"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m3" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m3"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m4" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m4"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m5" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m5"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m6" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m6"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m7" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m7"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m8" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m8"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template><template name="m9"><block wx:if="{{item.node === 'text'}}">{{item.text}}</block>
                    <view wx:if="{{item.node === 'element' && item.tag === 'view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </view>
            
                    <video wx:if="{{item.node === 'element' && item.tag === 'video'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </video>
            
                    <swiper wx:if="{{item.node === 'element' && item.tag === 'swiper'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </swiper>
            
                    <block wx:if="{{item.node === 'element' && item.tag === 'block'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </block>
            
                    <swiper-item wx:if="{{item.node === 'element' && item.tag === 'swiper-item'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </swiper-item>
            
                    <button wx:if="{{item.node === 'element' && item.tag === 'button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </button>
            
                    <slider wx:if="{{item.node === 'element' && item.tag === 'slider'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </slider>
            
                    <scroll-view wx:if="{{item.node === 'element' && item.tag === 'scroll-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </scroll-view>
            
                    <movable-area wx:if="{{item.node === 'element' && item.tag === 'movable-area'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </movable-area>
            
                    <movable-view wx:if="{{item.node === 'element' && item.tag === 'movable-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </movable-view>
            
                    <text wx:if="{{item.node === 'element' && item.tag === 'text'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </text>
            
                    <progress wx:if="{{item.node === 'element' && item.tag === 'progress'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </progress>
            
                    <checkbox-group wx:if="{{item.node === 'element' && item.tag === 'checkbox-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </checkbox-group>
            
                    <label wx:if="{{item.node === 'element' && item.tag === 'label'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </label>
            
                    <checkbox wx:if="{{item.node === 'element' && item.tag === 'checkbox'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </checkbox>
            
                    <form wx:if="{{item.node === 'element' && item.tag === 'form'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </form>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <input wx:if="{{item.node === 'element' && item.tag === 'input'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </input>
            
                    <radio-group wx:if="{{item.node === 'element' && item.tag === 'radio-group'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </radio-group>
            
                    <radio wx:if="{{item.node === 'element' && item.tag === 'radio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </radio>
            
                    <picker wx:if="{{item.node === 'element' && item.tag === 'picker'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </picker>
            
                    <picker-view wx:if="{{item.node === 'element' && item.tag === 'picker-view'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </picker-view>
            
                    <switch wx:if="{{item.node === 'element' && item.tag === 'switch'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </switch>
            
                    <textarea wx:if="{{item.node === 'element' && item.tag === 'textarea'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </textarea>
            
                    <navigator wx:if="{{item.node === 'element' && item.tag === 'navigator'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </navigator>
            
                    <audio wx:if="{{item.node === 'element' && item.tag === 'audio'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </audio>
            
                    <image wx:if="{{item.node === 'element' && item.tag === 'image'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" mode="widthFix">
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </image>
            
                    <map wx:if="{{item.node === 'element' && item.tag === 'map'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </map>
            
                    <canvas wx:if="{{item.node === 'element' && item.tag === 'canvas'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </canvas>
            
                    <contact-button wx:if="{{item.node === 'element' && item.tag === 'contact-button'}}" class="{{item.attr.className}}"width="{{item.attr.width}}"height="{{item.attr.height}}"data="{{item.attr.data}}"src="{{item.attr.src}}"id="{{item.attr.id}}"style="{{item.attr.style}}" >
                        <block wx:for="{{item.child}}" wx:key="{{item}}">
                            <template is="m9" data="{{item}}"/>
                        </block>
                    </contact-button>
            </template>